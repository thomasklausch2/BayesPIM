% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.bayespim.R
\name{summary.bayespim}
\alias{summary.bayespim}
\title{Summary method for bayespim objects}
\usage{
\method{summary}{bayespim}(object, warmup = object$warmup, probs = c(0.025, 0.5, 0.975), ...)
}
\arguments{
\item{object}{An object of class \code{"bayespim"}.}

\item{warmup}{Number of initial generated MCMC iterations to discard from
each chain before calculating the summary from every stored draw. It is
interpreted on the generated-iteration scale, not as a number of stored
draws when \code{save_every > 1}. Defaults to the final warmup value stored by
\code{bayespim()}, including any update or explicit override. Supplying this
argument overrides the stored value for this summary only.}

\item{probs}{Numeric vector of posterior quantiles.}

\item{...}{Additional arguments, currently unused.}
}
\value{
Invisibly returns a list of class \code{"summary.bayespim"} containing
the fitted latent-time distribution and incidence sampler, posterior and
convergence tables for the latent-time model and prevalence model, and
kappa when estimated, together with the convergence criteria, formatted
draw information, and the underlying draw counts. The same information is
printed to the console.
}
\description{
Summary method for bayespim objects
}
\examples{
data(mod)
summary(mod)

}
