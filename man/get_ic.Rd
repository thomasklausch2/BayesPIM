% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ic.R
\name{get_ic}
\alias{get_ic}
\title{Compute Information Criteria for a Bayesian Prevalence-Incidence Mixture Model}
\usage{
get_ic(mod, samples = NULL, cores = NULL)
}
\arguments{
\item{mod}{A fitted prevalence-incidence mixture model of class \code{bayespim}.}

\item{samples}{The number of MCMC samples to use. By default, all draws
available after the model's warm-up are used. If the model was fitted with
\code{save_every > 1}, this means all stored post-warm-up draws.}

\item{cores}{The number of cores for parallel processing using \code{foreach}. If \code{NULL} (default), all available cores will be used.}
}
\value{
A \code{matrix} containing WAIC-1, WAIC-2, and DIC values for the model.
}
\description{
Computes and returns information criteria for a fitted Bayesian prevalence-incidence mixture model, including the Widely Applicable Information Criterion 1 (WAIC-1), WAIC-2, and the Deviance Information Criterion (DIC). These criteria are commonly used for model comparison and evaluation in Bayesian analysis. See Gelman et al. (2014) for further details on these criteria.
}
\details{
This function calculates information criteria for a fitted Bayesian prevalence-incidence mixture model (\code{bayespim}). The information criteria include:
\itemize{
\item \strong{WAIC-1}: Based on the sum of posterior variances of log-likelihood contributions.
\item \strong{WAIC-2}: Similar to WAIC-1 but incorporates an alternative variance estimate.
\item \strong{DIC}: Measures model fit by penalizing complexity via the effective number of parameters.
}

The computation is performed by evaluating log-likelihood values for MCMC samples. By default, all MCMC samples after burn-in are used, though a subset can be specified via the \code{samples} argument. All incidence-time distributions supported by \code{\link[=bayespim]{bayespim()}} (Weibull, log-logistic, log-normal, gamma, and generalized gamma) are handled.
For a model fitted with covariate standardization, likelihood calculations
reconstruct the standardized parameterization and operate on standardized
covariates to reduce cancellation on large original scales; this
is algebraically equivalent to using the returned original-scale
coefficients.

Parallelization is available via the \code{foreach} package, utilizing multiple cores if \code{cores} is set accordingly. If \code{cores = NULL}, all available cores will be used.
}
\examples{
data(mod)
set.seed(2025)
get_ic(mod, samples = 20, cores = 1)

}
\references{
Gelman, A., Hwang, J., & Vehtari, A. (2014). Understanding predictive information criteria for Bayesian models. Stat Comput, 24(6), 997–1016.
}
