% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim_mcmc.R
\name{trim_mcmc}
\alias{trim_mcmc}
\title{Trim and thin an \code{mcmc.list}}
\usage{
trim_mcmc(obj, burnin = 0L, end = NULL, thinning = 1L)
}
\arguments{
\item{obj}{An object of class \code{mcmc.list}.}

\item{burnin}{Non-negative integer giving the number of stored rows to
discard. Defaults to \code{0}.}

\item{end}{Integer; final stored row to retain. Defaults to the number of rows
in the first chain.}

\item{thinning}{Integer; thinning interval. Defaults to \code{1}.}
}
\value{
An object of class \code{mcmc.list} containing the trimmed and thinned
chains.
}
\description{
Convenience function for trimming burn-in iterations and applying thinning to
an object of class \code{mcmc.list}.
}
\details{
The function selects stored rows
\code{seq(burnin + 1, end, by = thinning)} from each chain and reconstructs the
result as an \code{mcmc.list}, preserving its original iteration numbering and
multiplying its existing storage interval by \code{thinning}.
}
\examples{
data(mod)
trimmed <- trim_mcmc(
  mod$par,
  burnin = mod$warmup,
  thinning = 20
)
nrow(as.matrix(trimmed[[1]]))

}
