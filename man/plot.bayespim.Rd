% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.bayespim.R
\name{plot.bayespim}
\alias{plot.bayespim}
\title{Plot method for bayespim objects}
\usage{
\method{plot}{bayespim}(x, warmup = x$warmup, thinning = 1L, ...)
}
\arguments{
\item{x}{An object of class \code{"bayespim"}.}

\item{warmup}{Number of initial generated iterations to discard from each
chain before optional plot-only thinning. Defaults to the final warmup value stored by
\code{bayespim()}, including any update or explicit override. Supplying this
argument overrides the stored value for this plot
only.}

\item{thinning}{Positive integer thinning interval used only for this plot.
The default is no additional thinning. When the argument is omitted and
more than 20,000 post-warm-up draws are stored per chain, it is increased
automatically to limit plotting cost.}

\item{...}{Additional arguments passed to the MCMC plotting method.}
}
\value{
Invisibly returns \code{x}. The method is called for its side effect of
producing trace and density plots for the requested parameter blocks.
}
\description{
Plot method for bayespim objects
}
\examples{
data(mod)
plot(mod, thinning = 20)

}
